<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/keyboard_background">

    <!-- 候選字列 + 刪除鍵（統一位置） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end|center_vertical">

        <tw.igg.boshiamyime.ui.CandidateBarView
            android:id="@+id/candidate_bar"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:visibility="gone" />

        <TextView
            android:id="@+id/btn_candidate_delete"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="⌫"
            android:textSize="20sp"
            android:gravity="center"
            android:background="@drawable/key_ripple" />
    </LinearLayout>

    <tw.igg.boshiamyime.ui.KeyboardView
        android:id="@+id/boshiamy_keyboard_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>
